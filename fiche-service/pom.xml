<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>mg.esmia.miage</groupId>
        <artifactId>tsimokaai-platform</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>fiche-service</artifactId>
    <packaging>jar</packaging>
    <name>fiche-service</name>
    <description>
        Génération et stockage de fiches de révision, partage, annotations, validation enseignant.
        CRUD complet (fiches, partage, annotations, validation) : fourni et fonctionnel.
        La génération (pattern Map-Reduce : résumé intermédiaire par document puis fusion
        cohérente, cf. CDC §4.4) est implémentée dans FicheGenerationService (sortie
        structurée FicheContent via StructuredOutputValidationAdvisor, @CircuitBreaker).
    </description>

    <dependencies>
        <dependency>
            <groupId>mg.esmia.miage</groupId>
            <artifactId>common</artifactId>
        </dependency>
        <!-- Noyau partagé IA : bascule Groq/Gemini/Ollama (ChatProviderResolver) +
             starters Spring AI OpenAI/Ollama + resilience4j (transitifs). -->
        <dependency>
            <groupId>mg.esmia.miage</groupId>
            <artifactId>ai-common</artifactId>
        </dependency>
        <!-- Base vectorielle partagée (collection UNIQUE "chunks", Option A) : la phase MAP
             de la génération lit les chunks d'un document (payload content) pour produire
             les résumés intermédiaires — pas de dépendance à la BDD d'ingestion-service. -->
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-starter-vector-store-qdrant</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>${springdoc.version}</version>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>fiche-service</finalName>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
